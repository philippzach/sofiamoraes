import React from "react"
import Layout from "../components/layouts/index"

const NotFoundPage = () => (
  <Layout>
    <h1>NOT FOUND</h1>
    <p>
      Sorry this page could not be found. Hit the return button to get back
      where you started
    </p>
  </Layout>
)

export default NotFoundPage
